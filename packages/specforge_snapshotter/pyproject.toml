[project]
name = "snapshotter"
version = "0.1.0"
description = "Snapshotter package for SpecForge (LangGraph-based repo snapshot artifacts)"
readme = "README.md"
requires-python = ">=3.11,<3.13"
authors = [{ name = "stepan-o", email = "stepanoskin007@gmail.com" }]
dependencies = [
    # Add real deps after migration (langgraph, httpx/openai client, boto3, pydantic, etc.)
]

[project.scripts]
snapshotter = "snapshotter:main"

[build-system]
requires = ["uv_build>=0.9.7,<0.10.0"]
build-backend = "uv_build"
